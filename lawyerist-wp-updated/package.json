{
  "name": "sass-minimal",
  "version": "1.0.0",
  "description": "A minimal sass compiler configuration.",
  "main": "index.js",
  "devDependencies": {
    "sass": "^1.49.7"
  },
  "scripts": {
    "dev": "sass --watch --update --style=expanded assets/sass:assets/css",
    "build": "sass --no-source-map --style=compressed assets/sass:assets/css"
  },
  "author": "Sam Glover",
  "license": "UNLICENSED"
}